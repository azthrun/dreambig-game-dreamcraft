# Godot 4+ specific ignores
.godot/
.import/
/android/
export.cfg
export_presets.cfg
mono_crash.*.json

# Godot C#/.NET build artifacts
# Retained even though the project is GDScript-only: the installed Godot is the
# Mono build, so these appear if a C# hotspot is ever introduced (see SPEC.md).
bin/
obj/
*.sln.DotSettings.user

# IDE settings
.vs/
.idea/
.vscode/

# macOS
.DS_Store
